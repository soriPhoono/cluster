apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openbao
  namespace: openbao
spec:
  interval: 5m
  chart:
    spec:
      chart: openbao
      version: "1.0.0"
      sourceRef:
        kind: HelmRepository
        name: openbao
        namespace: flux-system
      interval: 1m
  values:
    server:
      service:
        annotations:
          tailscale.com/expose: "true"
