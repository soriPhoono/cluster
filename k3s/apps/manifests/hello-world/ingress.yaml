apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hello-world
  namespace: hello-world
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`hello-world.cryptic-coders.net`)
      services:
        - name: hello-world
          port: 80
---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: hello-world-tunnel-binding
  namespace: hello-world
subjects:
  - name: wildcard
    spec:
      fqdn: "*.cryptic-coders.net"
      # Must match Traefik entryPoints on the IngressRoute (`web` == :80). HTTPS :443 would hit `websecure`, which has no route → gateway/404.
      target: http://traefik.traefik.svc.cluster.local:80
tunnelRef:
  kind: ClusterTunnel
  name: guenivir-testing-tunnel
