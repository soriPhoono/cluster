apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hello-world
  namespace: hello-world
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`hello-world.cryptic-coders.net`)
      services:
        - name: hello-world
          port: 80
