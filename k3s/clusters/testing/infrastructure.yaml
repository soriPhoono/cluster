apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-testing
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 2m
  timeout: 15m
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./k3s/infrastructure/testing
  prune: true
  wait: true
